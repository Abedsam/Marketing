# scrollcraft fingerprint registry

| Build | Grammar | Nav treatment | Hero device | Act-sequence shape | Close pattern | Signature move |
|---|---|---|---|---|---|---|
| direct-line | Multi-page editorial site (not a single-scroll cinematic grammar — a real B2B service business with distinct pages) | Fixed minimal top bar (wordmark + links + phone + CTA), plus a persistent bottom trace rail as secondary jump-nav on the home page | Two-column: massive weight-400 headline + copy on the left, a generative Three.js particle constellation (no photography/video) on the right | Home page: hero → stats → 3 card-less services → partner marquee → review marquee → plain CTA band → footer (~5-6 scroll acts, no pinning, no scrub video) | Plain typographic CTA band bordered by hairlines, no dark panel, one filled button + one ghost link | "Direktverbindung"-Trace: a fixed bottom rail that draws a literal violet line as you scroll, stamping a labelled stop at each section, doubling as click-to-jump navigation |

First build in this registry — nothing to gate against yet.
